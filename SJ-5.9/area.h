#ifndef __AREA_H__
#define __AREA_H__
#include <cmath>

double area(double radius = 0);
double area(double length, double height);
double area(double bootom, double top, double height);
double area(double edgel, double edge2, double edge3, int);

#endif 
